# Copy to .env (and fill in real values) before running the backend with
# AI features enabled. The /api/chat endpoint stays disabled in the prototype
# even when this is set; the LLM wrapper (backend/app/llm.py) is the consumer.

ANTHROPIC_API_KEY=
